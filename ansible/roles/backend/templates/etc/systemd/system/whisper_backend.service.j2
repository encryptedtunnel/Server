[Unit]
Description=[Whisper] FastAPI Gunicorn App
After=network.target
[Service]
User={{ BACKEND_USER }}
Group={{ BACKEND_GROUP }}
WorkingDirectory={{ APP_DIR }}
ExecStart=/opt/gunicorn_start.sh
Restart=always
RestartSec=3
[Install]
WantedBy=multi-user.target

